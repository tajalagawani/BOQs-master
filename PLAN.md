# IOX — KPI Closure Plan

Sequenced plan to move all **24 KPI sub-components** from `BACKLOG.md` toward
🟢 sign-off, ordered by risk + leverage. Each milestone bundles related KPIs so
deliverables ship together rather than one-at-a-time.

| Status legend | Meaning |
|---|---|
| 🟢 Done | KPI met + evidence in place |
| 🟡 Partial | substance exists, evidence missing |
| 🟠 Weak | minimal coverage |
| 🔴 Not started | no work begun |

**Today:** 4 🟢 · 14 🟡 · 3 🟠 · 3 🔴
**Plan target:** 21 🟢 · 0 🟡 · 0 🟠 · 3 🔴 (the 3 left = external pen test + 1 deferred refactor)

---

## Milestone 1 — Documentation pack
**~4 hours · zero impact on the running app · closes 9 KPIs**

Build out the formal artefacts the sign-off matrix asks for. Everything lives
under `docs/` in the repo. No code touched.

**Artefacts shipped:**
- `docs/architecture/architecture-pack.md` — logical + physical diagrams, component model
- `docs/architecture/decisions/0001-*.md` … (ADRs for the key choices: Prisma+Drizzle coexistence, NextAuth at root, namespace isolation for ProcureX, mock-session shim, HeroUI Pro license)
- `docs/architecture/shared-data-model.md` — schema diagram showing both ORMs sharing one Postgres + cross-module entities
- `docs/security/access-control-matrix.md` — UserRole × Module × Action grid
- `docs/security/configuration-evidence.md` — exported Azure NSG rules + .env keys (redacted) + pm2 user mapping
- `docs/governance/raci.md`
- `docs/governance/release-procedure.md` (formalises what `deploy.yml` already does)
- `docs/governance/support-model.md` (escalation path, on-call, response SLAs)
- `docs/governance/change-control.md` + GitHub Issues template at `.github/ISSUE_TEMPLATE/`
- `docs/operations/environment-inventory.md` — VM SKUs, regions, IPs, tags, owners
- `docs/operations/release-notes/2026-05-31-go-live.md` — first release notes + go-live approval template

**KPIs closed (🟡 → 🟢):**
C1-SC1, C1-SC3, C2-SC1, C2-SC2, C4-SC1, C4-SC2, C4-SC4, C4-SC5, C5-SC3

**Dependencies:** none. Can start immediately.

---

## Milestone 2 — Test infrastructure
**~3 hours · additive (CI-only) · closes 2 KPIs**

Wire Vitest, write a smoke + integration suite, run it in CI. No production
runtime impact.

**Artefacts shipped:**
- `web/vitest.config.ts`
- `web/tests/unit/{calculations, dropdownOptions, formatters}.test.ts` (pure-function tests for the calculation helpers + data shapers)
- `web/tests/integration/{home,costx,procurex}.test.ts` (Playwright or HTTP-based — hit each major route, assert 200 + key content)
- `web/tests/integration/extraction-pipeline.test.ts` (upload → enqueue → drain → verdict shape — uses mock Anthropic)
- `.github/workflows/test.yml` (runs on every PR + on push to main BEFORE deploy.yml)
- `docs/testing/test-strategy.md` + `docs/testing/last-execution-report.md` (auto-generated by CI)

**KPIs closed (🔴/🟡 → 🟢):**
C2-SC3, C3-SC3

**Dependencies:** none.

---

## Milestone 3 — Infrastructure as Code
**~2 hours · additive (doesn't replace running infra) · closes 1 KPI**

Re-express the existing Azure resources as Terraform. The running VM stays
untouched; Terraform imports it so future changes are version-controlled.

**Artefacts shipped:**
- `infra/terraform/{main.tf, variables.tf, outputs.tf, terraform.tfvars.example}`
- Defines: resource group, VM, NIC, public IP, NSG with all rules, OS disk
- `infra/terraform/import.sh` — one-shot script that `terraform import`s the existing 7 resources (zero recreation)
- `infra/terraform/README.md` — how to apply / plan / state-locking via Azure Storage backend
- `docs/infrastructure/iac-evidence.md` — output of `terraform plan` proving zero-diff against current infra

**KPIs closed (🟡 → 🟢):**
C1-SC2

**Dependencies:** none. Won't run `terraform apply` on the live infra until proven zero-diff.

---

## Milestone 4 — API spec + integration evidence
**~2 hours · zero impact · closes 3 KPIs**

OpenAPI for every Next.js route + server action. Integration capability proven
through documented examples.

**Artefacts shipped:**
- `docs/api/openapi.yaml` — generated from `app/**/route.ts` and server actions, hand-augmented with descriptions
- `docs/api/integration-architecture.md` — request flow diagram, auth flow, AI extraction pipeline
- `docs/integrations/anthropic.md` — bidirectional transaction evidence (FoT extraction trace: request → agent loop → verdict)
- `docs/integrations/third-party-onboarding.md` — step-by-step for a new 3rd party to integrate (auth, API key, webhooks)

**KPIs closed (🟡 → 🟢):**
C3-SC1, C3-SC4, C3-SC5

**Dependencies:** none.

---

## Milestone 5 — HTTPS + monitoring + load test (production hardening)
**~4 hours · low-risk additive · closes 4 KPIs**

The only milestone that touches the live VM. All changes are additive: HTTPS
adds 443 alongside 80, monitoring just observes, load test just measures.

**Artefacts shipped:**
- nginx config update: 443 with Let's Encrypt cert (`certbot --nginx`), 80 → 443 redirect
- Sentry SDK wired into `web/app/layout.tsx` + `web/sentry.{client,server}.config.ts` (free tier ≥ 5k events/mo)
- Azure Monitor VM Insights extension enabled on `iox-vm-01` (CPU/RAM/disk/network dashboards)
- `web/scripts/load-test.js` (k6 script) + `docs/performance/load-test-report.md` (baseline numbers: RPS, p50/p95/p99, error rate, peak RAM)
- `docs/operations/monitoring-runbook.md` — alert thresholds, escalation, common incidents
- `docs/operations/uptime-report.md` — auto-updates from Azure Monitor weekly

**KPIs closed:**
- C1-SC4 (🟡 → 🟢) HTTPS enabled
- C5-SC1 (🟠 → 🟢) Sentry + Azure Monitor active
- C5-SC2 (🔴 → 🟢) k6 baseline + report
- C5-SC5 (🟠 → 🟢) — needs **7 days of uptime evidence**, so this one closes asynchronously a week after the milestone ships

**Dependencies:**
- HTTPS needs a domain (e.g. `iox.example.com`) pointing at `20.203.125.83`. If no domain yet, postpone HTTPS only — everything else can ship.
- Sentry needs a free Sentry.io account + project key (you pull the DSN, paste it).

---

## Milestone 6 — QA + readiness reports
**~1 hour · pure documentation · closes 1 KPI**

Roll up everything above into the assessor-facing report.

**Artefacts shipped:**
- `docs/quality/defect-log.xlsx` — open template + populate with the bugs we've fixed (BOQ getCell crash, AUTH_TRUST_HOST, HeroUI Pro postinstall, FoT empty extraction, NextAuth cookie stale, etc.)
- `docs/quality/production-readiness-report.md` — one-page summary referencing all artefacts from M1–M5
- `docs/quality/sign-off-matrix.md` — re-do `BACKLOG.md` as a table that an assessor can tick row-by-row

**KPIs closed (🟡 → 🟢):**
C5-SC4

**Dependencies:** M1–M5 done (or partially — defect log can grow incrementally).

---

## Milestone 7 — External pen test (the only spend)
**External vendor commission · 1–3 weeks lead time · closes 1 KPI**

The framework explicitly requires "independent security validation". Can't be
self-served.

**Artefacts shipped:**
- Engagement scope: web app + Azure infra
- Vendor options to evaluate: NCC Group, Pentest People, Cobalt.io (PtaaS), or a UAE-local firm
- `docs/security/pen-test-report.pdf` (from vendor) + `docs/security/remediation-log.md` (our response)

**KPI closed (🔴 → 🟢):** C1-SC5

---

## Deferred — Microservices refactor
**🔴 → stays 🟠 by design**

**C2-SC4 Modular service design** requires modules to be **independently
deployable**. Today everything is one Next.js process. Splitting into
microservices is a 2–4 week refactor that breaks the single-deploy model and
isn't justified by current traffic.

**What we'll do instead:** document the route-group + module-folder isolation
we already have, accept "Weak" on this KPI with a note that physical
modularity is a future programme increment if scale demands it.

---

## Suggested order + cadence

| Day | Milestone | Outcome |
|---|---|---|
| **D+1 (today)** | M1 documentation pack | 9 KPIs go 🟡→🟢 in a single PR. No code touched. |
| **D+2** | M2 test infrastructure + M3 IaC | CI grows a test stage; Terraform mirrors the live infra. |
| **D+3** | M4 API spec + integration evidence | OpenAPI + integration docs. |
| **D+4** | M5 production hardening (HTTPS + Sentry + load test) | Live VM gets TLS + monitoring + a measured baseline. |
| **D+5** | M6 QA + readiness reports | Assessor-ready bundle. |
| **D+12** | C5-SC5 closes automatically | 7-day uptime evidence accrues. |
| **D+14 – D+21** | M7 pen test report returns | C1-SC5 closes. |

**End state at D+5:** 19 of 24 KPIs at 🟢.
**End state at D+21:** 23 of 24 KPIs at 🟢 (only C2-SC4 deferred, with documented rationale).

---

## What I need from you to start

| Item | Why | Default |
|---|---|---|
| Domain name pointing at `20.203.125.83` | HTTPS via certbot | Skip → defer HTTPS only, ship the rest of M5 |
| Sentry DSN (free account at sentry.io) | C5-SC1 monitoring | Skip → use Azure Monitor alone (less rich but no signup) |
| Pen test vendor preference | M7 commission | I'll propose 2 options and you pick |
| Approval to update Vitest config in `web/` | M2 test suite | Implicit if you say "go" |

Say "go on M1" / "go on M1–M4" / "go on everything except HTTPS" — I'll start
the chosen milestones in one batch.
